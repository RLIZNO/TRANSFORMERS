/*Constantes para los select que viene con valores por default */
(function () {
    'use strict';

    angular
        .module('app')
        .constant('SELECT_DEFAULT', {
            TYPE_DOCUMENTS: 2 /** Id que contiene el tipo de documento cc */
        });
})();